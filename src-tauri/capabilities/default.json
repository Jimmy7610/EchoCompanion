{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "EchoCompanion default capabilities — core UI + updater check/install only. No shell, no broad FS.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
